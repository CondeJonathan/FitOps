# FitOps Team Template

Use these two files as shared coordination docs:

- `frontend/README.md` for all frontend updates
- `backend/README.md` for all backend updates

Both frontend and backend folders are scaffolded with `.gitkeep` placeholders so the structure stays organized in git.
